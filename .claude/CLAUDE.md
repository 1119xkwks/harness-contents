# Claude Code 프로젝트 가이드

## Skills (슬래시 명령어)

### `/projs-setup`
관리자 FE, BE 페이지를 자동으로 생성합니다.
- 레이아웃, 컴포넌트, 페이지 모두 자동 생성
- 프로젝트 생성 위치: `projs/fe-admin`, `projs/be-admin` 디렉터리

## BE 개발 서버 실행

```bash
cd projs/fe-admin && npm run dev -- --port 3001
```

완료 후 http://localhost:3001에서 대시보드를 확인할 수 있습니다.
